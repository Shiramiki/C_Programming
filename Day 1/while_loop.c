#include <stdio.h>

int main(){
    //This program prints multiples of ten from 0 to 1000 using a while loop
    int count;
    while (count <= 1000){
        printf("%d\t",count);
        count+=10;
    }
}